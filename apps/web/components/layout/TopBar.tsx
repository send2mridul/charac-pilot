"use client";

/**
 * TopBar is no longer used globally. Each page now owns its own header
 * matching the Lovable design pattern. This file is kept as a no-op
 * export so any remaining imports do not break.
 */
export function TopBar() {
  return null;
}
